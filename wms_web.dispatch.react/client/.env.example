# API Configuration
# For development, you can use the proxy (set to /api) or direct URL
# For production, always use the full URL

# Production API URL (used when NODE_ENV=production)
REACT_APP_API_BASE_URL=https://plankton-app-d86df.ondigitalocean.app/api

# Development API URL (optional, defaults to proxy if not set)
# Uncomment below to use direct URL in development instead of proxy
# REACT_APP_DEV_API_URL=http://localhost:5000

# API Timeout in milliseconds
REACT_APP_API_TIMEOUT=15000
